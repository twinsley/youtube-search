Talked with Github Copilot a bit to help with a starting point in figuring out options to research further for the router. It suggested Lit Router, along with a few alternatives to look into.

Used AI autocomplete (note, not agent mode, just autocomplete in VS Code) when its suggestions happened to be what I was intending to type anyway. Mostly just on the boilerplate to get a component started.

Some usage of AI as a quick check for anything obvious I was missing when something wasn't working right. Mostly just used as a rubber duck as far as debugging goes.

Used some AI assistance to help speed up getting the CSS figured out (It was nothing I haven't done before, but it's been a bit so I was slightly rusty. Having the AI remind me was faster than googling in this case). Again all in a conversational mode where at most it suggested code, some of which I used; not agent mode.

Used AI to generate the SVGs for a few icons (e.g. the bookmark icon).