import { LitElement, css, html } from 'lit'
import { customElement, property } from 'lit/decorators.js'

@customElement('nav-bar')
export class NavBar extends LitElement {
    //TODO implement nav bar for use on at least the result page. Should have a link to the home page, maybe a search bar? Plus bookmarks link if I get that far.
}